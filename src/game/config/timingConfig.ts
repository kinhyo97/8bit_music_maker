/**
 * 시간 기반 플레이 감각을 조절하는 공용 설정이다.
 * 노트가 언제 보이기 시작하고, 홀드 종료를 얼마나 관대하게 볼지 같은 값을 모아둔다.
 */
export const timingConfig = {
  // 노트가 판정선에 도달하기 몇 초 전에 화면에 등장할지 정한다.
  approachTime: 1.6,
  // 플레이 시작 직후 유저가 준비할 수 있도록 두는 선행 여유 시간이다.
  leadInTime: 1.0,
  // 홀드 종료 시 너무 칼같이 떼지 않아도 되도록 허용하는 보정 범위다.
  releaseGraceMs: 90,
} as const;
